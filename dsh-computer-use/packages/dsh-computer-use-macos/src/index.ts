/**
 * macOS package entry (unpublished draft).
 *
 * This is the future thin wrapper for the macOS provider. It is NOT built or
 * published yet; the actual Windows package remains the only released package.
 */
export const name = 'dsh-computer-use-macos'
export const platform = 'darwin'
